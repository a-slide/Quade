#########################################################################################
#                                                                                       #
#                            QUADE CONFIGURATION FILE                                   #
#                                                                                       #
#########################################################################################                                                    

#   Values can by customized with users values, but the file template must remain unchanged, otherwise the program will not be able to load default values.
#   Please follow the following recommendations :
#   - File path should be indicated as absolute path preferably and should not contain blank spaces
#   - Values identified with '**' in the descriptor are not recommended to be modified

##########
[general]
##########

# Path where to create a folder in which all files generated during the program will be stored. Can be leaved empty if you want to work in the current directory (string)
outdir : ./my_folder

##########
[quality]
##########

# Minimal mean quality of the index to consider a read pair valid (between 0 and 40). 0 if no filtering requested. (INTEGER)
mean_qual : 30

# Minimal quality for one base of the index to consider a read pair valid. 0 if no filtering requested. (INTEGER)
minimal_qual : 20

# Quality format associated with reads autorized values are : 'fastq-sanger' 'fastq-solexa' or 
# 'fastq-illumina'. See Biopython QualityIO documentation for more details. (STRING)
quality_scale : fastq_sanger

##########
[index]
##########

# For each possible index indicate by a boolean its usage or not. If not you can lease the position coordinates empty, else indicate the start and end positions within in index read using 0 base coordinates

# Sample index 1
# True if in usage else False
index1 = True
#start and end positions (INTEGER)
index1_start : 0
index1_end : 5

# Sample index 2 start and end positions [if double indexing] (INTEGER)
index2 = True
index2_start : 0
index2_end : 5

# Molecular index 1 if stochastic index was added after sample index 1 (INTEGER)
molecular1 = False
molecular1_start :
molecular1_end :

# Molecular index 2 if stochastic index was added after sample index 2 (INTEGER)
molecular2 = False
molecular2_start :
molecular2_end :

###############################
#    SAMPLE DEFINITIONS       #
###############################

# It is possible to include as many independant sample as required by duplicating a entire sample section and incrementing the id number in the sample section name

[sample1] 
# basename that will be used to prefix the read files (STRING)
name : S1
# Index sequences associated with the sample. Index 2 is used only in case of double indexing (STRING)
index1_seq : ATCGT
index2_seq : TTGTG

[sample2]
# basename that will be used to prefix the read files (STRING)
name : S2
# Index sequences associated with the sample. Index 2 is used only in case of double indexing (STRING)
index1_seq : ATCCT
index2_seq : TTGGG

